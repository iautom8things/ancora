# Decisions

Run `mix spec.decision.new <decision-id> --title "Decision title"` to add an
ADR. Name the affected subject or requirement IDs in `affects:` and complete
the Context, Decision, and Consequences sections.
