{
  "assignee": "Manuel Zubieta",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-09-04T11:03:31Z",
  "description": "Advances: ancora.findings, ancora.gate\n\nFindings: F5 (High, data H1 + spec H1, consensus CF2), F16 rides along (Medium, idioms M7 + simplicity M3). Consensus CF2 in ~/vaults/rmbrs-vault/code_review/critical/ancora/2026-09-04/ancora-xla-post-ship-hardening/20260904-061300-consolidated.md.\n\nDeliverable:\nlib/ancora/trailer.ex:71-84 folds `git log \u003cbase\u003e..HEAD` output (newest first) with Map.merge(acc, new), so when two commits acknowledge the same code at different severities the OLDEST commit wins. non_tip_overrides is then computed by dropping keys the tip mentions rather than by comparing applied values, so the code is excluded regardless of which severity won. Reproduced: tip declares derived/drift=warning, an older commit declares info; Trailer.read returns info with empty non_tip_overrides; the gate prints result=pass with no warning; after squash the tree resolves warning and fails on trunk. That is the post-merge verdict flip ancora.decision.durable_acknowledgments exists to prevent, with the warning ancora-sfu added suppressed by construction. Neither ancora.findings.trailer_grammar nor ancora.gate.acknowledgment_clears defines precedence for the same code across commits, so it is a spec defect first and a code defect second.\nFix: reverse the fold so the commit nearest HEAD wins; compute non_tip_overrides by applied severity (keep a code when its winning value differs from what the tip supplies, or when the tip does not name it); replace the second Severity.resolve_all/2 pass in Gate.warn_non_tip_acknowledgments (gate.ex:494-508) with the direct check that the applied trailer value is what differs (F16; the second resolution is the duplication the simplicity and idioms reviewers both named). Define the precedence rule in the spec.\n\nVerification:\n- Tests to pass: test/ancora/trailer_test.exs, test/ancora/gate_test.exs, test/mix/tasks/spec_check_test.exs\n- Behaviors to demonstrate through real git fixtures and the real CLI:\n  1. Tip warning, older info, same code: the gate resolves warning; result reflects warning; no squash-loss warning (nothing would be lost). Mutation: restore the fold direction; the test must fail.\n  2. Tip silent, older info, config or default stricter: the gate resolves info AND prints the squash-loss warning; after squash the trunk run resolves the stricter value. Mutation: compute non_tip_overrides by key; the test must fail.\n  3. The four states from ancora-sfu (equal config silent, stricter config loud, no config loud, tip repeat silent) still hold byte-for-byte on stderr. Mutation: delete the F16 replacement and the old conjunction together; at least one must fail.\n- Spec edit required in-diff: ancora.findings.trailer_grammar shall state that when the same code is acknowledged in more than one commit of the range, the commit nearest HEAD wins; ancora.gate.acknowledgment_clears gains a scenario for conflicting severities across commits (given/when/then with concrete values). Decision-record edit: ancora.decision.durable_acknowledgments Decision or Consequences section records the precedence.\n- Verification cadence:\n  - Iteration tier: compile-graph-scoped checks only (`mix test --stale` or the named files) plus `mix spec.validate`; run `mix spec.check --base e18fa05` as a dev-loop check as soon as the spec edit exists, before either capped final-tier run.\n  - Final tier: immediately before IMPLEMENTED, `mix compile --warnings-as-errors`, the named test files, `mix format --check-formatted`, `mix deps.unlock --check-unused`, `mix spec.check --base e18fa05`, run-until-green. Full suite runs at the orchestrator's merge gate.\n  - Verifier tier: `mix spec.check --base e18fa05` plus targeted selections; no full suite.\n- Standing bounce-class checks:\n  - Production call site named for each new callable surface, with the proving test that exercises that production path\n  - Every conditional clause covered by a test that fails under the named mutation; prove each ordered test both ways and report the numbers\n  - Spec surfaces kept fresh: the substantive requirement or scenario edit named below lands in the same diff with its decision-record edit; never a Spec-Ack trailer; never weaken a must\n  - Fixtures never call production functions (use test/support or System.cmd); under file-scoped derivation a stray call binds a subject\n  - Grader scratch discipline applies to you too: work only in this worktree, address it by absolute path\n- Call-site Integration: Trailer.read/2 is called from Gate.check/2 (and Status); prove through the CLI fixtures and the gate test, not the trailer unit test alone.\n\nOut of Scope (files):\n- lib/ancora/config.ex (F7 is another child), lib/ancora/review/**, lib/ancora/parser.ex, lib/ancora/verifier.ex, lib/ancora/git/**, lib/ancora/derive/**, lib/mix/tasks/**\nAllowed touches (this ticket may modify):\n- lib/ancora/trailer.ex, lib/ancora/severity.ex (only if the applied-value comparison needs a helper)\n- lib/ancora/gate.ex (warn_non_tip_acknowledgments and its call site only)\n- test/ancora/**, test/mix/tasks/**, test/support/**\n- .spec/specs/ancora.findings.spec.md, .spec/specs/ancora.gate.spec.md (substantive edits named above; never weakening a must)\n- .spec/decisions/ancora.decision.durable_acknowledgments.md\n- Any .spec/specs/\u003csubject\u003e.spec.md that `mix spec.check --base e18fa05` names in a derived/drift, derived/growth or derived/shrink finding caused by THIS ticket's own diff (binding-aware clause: derivation is file-scoped, so acknowledgment edits only, one substantive sentence describing what this ticket changed as that subject observes it, verified by execution; if the subject observes nothing, say so in the completion record and stop)\n- .spec/decisions/** (every spec edit requires a decision-record edit in the same diff; write a real contract statement, never a hollow line)\n\nOut of Scope (intent):\n- The trailer grammar stays info|warning only, no off, no presets. No announcement of tip-commit acknowledgments (F41, deferred). Do not read the merge commit body. Do not rebase; base stays e18fa05.\n\nMerge gate:\n- [ ] Code compiles cleanly\n- [ ] Tests listed above pass\n- [ ] Spec verification passes (`mix spec.check --base e18fa05` result=pass)\n- [ ] Scope check: diff touches only Allowed globs (mechanical)\n- [ ] Auditor APPROVE comment on the ticket",
  "id": "ancora-41r.3",
  "labels": [
    "critical",
    "phase:verifying"
  ],
  "parent": "ancora-41r",
  "comments": [
    {
      "text": "IMPLEMENTED\nSession: 41r3-i1-adec463e\nLog: /Users/mz/.local/state/agentic-runs/ancora/ancora-41r.3/codex-41r3-i1-adec463e/log\n\nCommit: a56fd932b29ed2aa599d0d1306c1bed056fade34\nFiles touched: .spec/decisions/ancora.decision.durable_acknowledgments.md, .spec/specs/ancora.findings.spec.md, .spec/specs/ancora.gate.spec.md, lib/ancora/gate.ex, lib/ancora/trailer.ex, test/ancora/trailer_test.exs, test/mix/tasks/spec_check_test.exs\nTests added: Mix.Tasks.Spec.CheckTest \"the tip acknowledgment wins when commits name conflicting severities\", \"a non-tip acknowledgment changes the result after squash\", and \"repeating the non-tip acknowledgment on the tip stays silent\"; updated Ancora.TrailerTest \"the nearest override wins and keeps a differing non-tip value\"\nSpec acknowledgment: substantive edits to ancora.findings and ancora.gate, with the precedence decision recorded in ancora.decision.durable_acknowledgments; no Spec-Ack trailer used\nVerification tier: change-scoped — mix compile --warnings-as-errors; test/ancora/trailer_test.exs, test/ancora/gate_test.exs, test/mix/tasks/spec_check_test.exs; mix format --check-formatted; mix deps.unlock --check-unused; mix spec.check --base e18fa05; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\n\nSummary: Trailer.read now gives the commit nearest HEAD precedence and retains the nearest differing below-tip value, while Gate warns only when that below-tip value supplied the finding severity and differs from durable config or the default. Mutation proof: item 1's oldest-wins fold produced 1 failure from 1 selected CLI test and passed 1 of 1 after revert; item 2's key-based non-tip filter produced 1 failure from 1 selected trailer test and passed 1 of 1 after revert; item 3's old source-and-membership predicate produced 2 failures from 2 selected CLI tests and passed 2 of 2 after revert. The final 67-test run passed and pins equal config silent, stricter config loud, no config loud, and tip repeat silent byte-for-byte on stderr.",
      "timestamp": "2026-09-04T11:30:09Z"
    },
    {
      "text": "TEST_CRITIC: FAIL\n\nBlocking: the nearest-below-tip ordering introduced by this diff's own must-level spec edit has no covering test, and its failure mode is the ticket's own bug.\n\nFAILING ITEM\n\nlib/ancora/trailer.ex non_tip fold, second `Enum.reverse()`: no test in the repo has two conflicting below-tip trailer commits. Every fixture in test/ancora/trailer_test.exs, test/ancora/gate_test.exs and test/mix/tasks/spec_check_test.exs carries exactly one below-tip commit that names a code, so the below-tip fold direction is never discriminated.\n\nancora.findings.trailer_grammar (priority: must) now states the read result \"shall identify the nearest valid value below the tip\". Delete that second `Enum.reverse()` and `overrides` still resolves nearest while `non_tip_overrides` reverts to oldest-wins. Fixture: C1 `Spec-Ack: derived/drift=warning`, C2 `Spec-Ack: derived/drift=info`, C3 tip with no trailer, default severity error. finding.severity resolves :info from the nearest; non_tip_severity becomes :warning from the oldest; the gate's `finding.severity == non_tip_severity` conjunct goes false; the squash-loss warning is suppressed and the trunk run after squash flips to error. That is F5 one layer down, and the full suite stays green.\n\nMissing test: a CLI fixture with two conflicting below-tip acknowledgments and a bare tip, asserting `stderr == @squash_warning` on the branch and the stricter resolution after squash.\n\nUNVERIFIED BY READING: that the suppression actually reaches stderr under this mutation. Mutation for the verifier: drop `Enum.reverse()` from the `non_tip_overrides` pipeline in lib/ancora/trailer.ex (keep the one in `overrides`); expect 0 failures across the three named files.\n\nPER-ASSERTION VERDICT\n\n1. \"the tip acknowledgment wins when commits name conflicting severities\" (spec_check_test.exs:385) PASS. Commits are built older-first: `Spec-Ack: derived/drift=info` on the earlier commit, `=warning` on the tip. Trailer.read reads `git log \u003cbase\u003e..HEAD` (newest first), so List.first is genuinely the tip. Resolved severity asserted by equality off the JSON report (`drift[\"severity\"] == \"warning\"`, `severity_source == \"trailer\"`), status 1, last stdout line pinned in full, and `stderr == \"** (Mix) spec.check failed\\n\"` by equality so any squash-loss line breaks it. Under the old oldest-wins fold this resolves info, status 0, verdict pass: three assertions fail.\n\n2. \"a non-tip acknowledgment changes the result after squash\" (spec_check_test.exs:415) PASS on structure. It creates a real `feature` branch, commits the info trailer below a bare tip, runs the real CLI, then `git checkout main`, `git merge --squash feature`, a real trunk commit with no trailer, and re-runs the CLI against the same `--base`. Branch half: severity info / source trailer by equality, `stderr == @squash_warning` byte-for-byte. Trunk half: severity error / source default by equality, errors=1, `stderr == \"** (Mix) spec.check failed\\n\"`. Real squash, real re-run.\n\n3. \"repeating the non-tip acknowledgment on the tip stays silent\" (spec_check_test.exs:460) PASS. This is the genuine tip-repeat-at-the-same-severity state, which existed nowhere before: the pre-existing trailer unit test claimed it by name but its fixture used differing severities. `stderr == \"\"` is the behavioral guard on the `Map.reject` equal-value drop; remove that reject and the warning fires and this fails.\n\n4. trailer_test.exs:154 \"the nearest override wins and keeps a differing non-tip value\" PARTIAL. It pins nearest-wins (`overrides[\"derived/drift\"] == :info` with earlier=warning, tip=info) and differing-retained (`non_tip_overrides == %{\"derived/drift\" =\u003e :warning}`). It does NOT pin the equal-dropped half. That half is guarded only behaviorally by CLI item 3 above. Non-blocking, but the unit test does not pin both halves as the ticket asked.\n\n5. Key-based non_tip mutation: it fails ONLY trailer_test.exs:154. None of the three CLI tests catches it, and none can. Under nearest-wins the two computations are gate-indistinguishable. When the tip names the code, value-based keeps a differing below-tip value but the gate's `finding.severity == non_tip_severity` conjunct is then false, so silent; key-based drops it, also silent. When the tip omits the code, both keep it. So the ticket's item-2 mutation is proven by a unit assertion on a returned map, not \"through real git fixtures and the real CLI\" as the ticket framed it. Worth knowing: the differing-value retention has no observable consequence at the gate today.\n\n6. No weakening of the ancora-sfu tests. Name-list diff of e18fa05 vs a56fd93 shows zero tests removed and zero fixtures altered. Four assertions were TIGHTENED: `unpromoted.stderr =~ \"lost by a squash merge\"` and the two `refute ... =~` became `== @squash_warning` / `== \"\"`, and the higher-severity test likewise. Ordered unpromoted / promoted / trunk and the stricter-config case are intact.\n\n7. No assertion pins source text. `@squash_warning` pins subprocess stderr, which is the ancora.tasks.stderr_pinning contract. The pre-existing moduledoc assertion in trailer_test.exs is untouched by this diff.\n\n8. Reachability of the subprocess stderr assertions is VERIFIED, not assumed. `run_mix_subprocess` writes a shell wrapper redirecting fd 2 to a file. Positive (`== @squash_warning`) and negative (`== \"\"`) assertions coexist in the same file and both pass, so a silently-empty capture would fail the positive ones.\n\nRUNS\ntest/ancora/trailer_test.exs + test/mix/tasks/spec_check_test.exs: 35 tests, 0 failures.\ntest/ancora/gate_test.exs: 32 tests, 0 failures.",
      "timestamp": "2026-09-04T11:36:38Z"
    },
    {
      "text": "VERIFY: PASS\nSession: adec463e-8b46-45af-8018-b1cd8b92afc5\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-ancora/adec463e-8b46-45af-8018-b1cd8b92afc5.jsonl\n\nMechanical checks:\n- Files touched: 7\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0\n- lib/ancora/gate.ex: 7 insertions, 7 deletions. The body of warn_non_tip_acknowledgments plus one module-level `alias Ancora.Config` line. The call site (resolve_findings) is byte-identical to e18fa05, so the alias is the only line outside the named function; it is the import the new body requires.\n\nSemantic checks:\n- Spec gate: spec.check result=pass\n  (checked subjects=7 requirements=89 errors=0 warnings=0; branch base=e18fa05 changed_files=7 findings=7 total error=0 warning=0 info=7)\n- Zero `Spec-Ack:` trailers in e18fa05..HEAD, so nothing in the gate's pass was bought with an acknowledgment.\n- Requirements coverage:\n  - ancora.findings.trailer_grammar (must): covered by test/ancora/trailer_test.exs:154 plus nine other tagged tests in the same file, all against real git repos built by test/support/ancora_case.exs. Would fail if the fold direction reverted (asserts overrides[\"derived/drift\"] == :info against an older warning) or if non_tip_overrides went back to key-based dropping (asserts %{\"derived/drift\" =\u003e :warning}). Both proved below. Seams: none. init_git_repo/commit_all shell real git through System.cmd.\n  - ancora.gate.acknowledgment_clears (must): covered by test/mix/tasks/spec_check_test.exs:385, :415, :460, :486, :566 (real project fixtures, real `mix spec.check` subprocess) and test/ancora/gate_test.exs:508-644. Would fail if the gate emitted a squash-loss line when the tip supplies the winning acknowledgment (:385 pins stderr byte-for-byte), or stopped emitting it when the below-tip value differs from durable config or the default (:486, :566). Seams: none. run_mix_subprocess spawns the real mix binary against a real temp git repo.\n- Intent-level scope concerns: none. @severity_tokens and @rejected_severities are untouched, so the grammar is still info|warning with error and off rejected at parse. No tip-commit announcement (the :385 fixture's stderr is exactly \"** (Mix) spec.check failed\\n\"). Git.run still reads `log \u003cbase\u003e..HEAD --format=%B%x00`, never a merge body. mix.exs and mix.lock untouched. HEAD's parent is e18fa05, no rebase.\n- Targeted verification: pass.\n  - Stage worktree: `mix spec.check --base e18fa05`; `mix test test/ancora/trailer_test.exs test/ancora/gate_test.exs test/mix/tasks/spec_check_test.exs` -\u003e 67 tests, 0 failures.\n  - Mutations rebuilt from scratch in a disposable detached worktree, never in the stage tree:\n    1. Oldest-wins fold restored (Map.merge over the unreversed list): spec_check_test.exs:385 fails 1/1 on `result.status == 1` (left 0). Reverted -\u003e 1/1 pass.\n    2. non_tip_overrides computed by key (`Map.drop(overrides, Map.keys(tip_overrides))`): trailer_test.exs:154 fails 1/1, left %{} right %{\"derived/drift\" =\u003e :warning}. Reverted -\u003e 1/1 pass. Worth knowing: this mutation leaves all five acknowledgment CLI tests green (0 failures from 5 selected), so the by-value non-tip set is a Trailer-level contract with no gate-observable consequence today. That is honest, not a hole: the requirement text names the read result, and the unit test calls production Trailer.read.\n    3. Old second Severity.resolve_all pass restored verbatim: spec_check_test.exs:385 fails 1/5 selected on stderr, emitting \"[CONFIG] Spec-Ack: derived/drift=info resolved from a non-tip commit...\" for a code the tip declared at warning. Reverted -\u003e 5/5 pass.\n  - Defining-clause loosenings on the new predicate, each proved separately (unused-variable warnings suppressed so stderr stayed clean; the stderr_pinning tests are tight enough that a compiler warning masquerades as a failure):\n    - drop `finding.severity == non_tip_severity` (the \"exists only in a non-tip commit\" clause): 1 failure, spec_check_test.exs:385.\n    - drop `finding.severity != durable_severity` (the \"would change the resolved severity\" clause): 1 failure, spec_check_test.exs:486.\n    - drop `finding.severity_source == :trailer`: 0 failures across all three named test files (43 + 24 selected). See Notes.\n  - Ordered behaviors measured myself with hand-built git fixtures driven through `mix spec.check --root ... --base ... --json`, not by reading the implementer's tests:\n    1. Tip `derived/drift=warning` over older `=info`: severity warning, severity_source trailer, verdict \"spec.check result=fail tier=branch errors=0 warnings=1\", stderr carries no squash-loss line. The same fixture at e18fa05 gives severity info and \"spec.check result=pass\" with empty stderr, which is the reported bug reproduced.\n    2. Tip silent over older `=info` with the default (error) stricter: branch run resolves info/trailer, \"spec.check result=pass\", stderr is exactly the squash-loss warning; after `git merge --squash` onto trunk the same tree resolves error/default and \"spec.check result=fail tier=branch errors=1 warnings=0\". The stricter-config variant behaves the same on the branch.\n    3. Four ancora-sfu states captured at both a56fd93 and e18fa05 from identical fixtures and compared with cmp: equal config silent, stricter config loud, no config loud, tip repeat silent are all byte-identical on stderr.\n- Whitelist adherence: change-scoped + diff-touched tests + spec gate only; compile/format/credo/deps re-establishment: not run\n- Corpus-scoped claims: n/a - code diff. Spec and ADR sentences validated by execution against the fixtures above.\n- Full-suite verification: not run by verifier (by design)\n- Audit-expanded scope: none\n\nSpec and decision-record truth:\n- ancora.findings.trailer_grammar now states \"When more than one commit in the range acknowledges the same code, the commit nearest HEAD shall win\" and describes the non-tip set by value (\"omit that below-tip value only when the tip repeats the same severity\"). Both match the code and the measured behavior. The requirement still says never error, never off, no presets: no must was weakened.\n- ancora.findings.scenario.non_tip_override_is_identified gained the conflicting-tip line; ancora.gate.scenario.nearest_commit_acknowledgment_wins is a full given/when/then with concrete values. I ran that scenario as a fixture: warning, severity_source trailer, result=fail, no squash-loss line. All three \"then\" clauses hold.\n- ancora.decision.durable_acknowledgments records the precedence in its Decision section as a real contract sentence, not a hollow line, and it is true of the shipped code.\n\nVerdict: PASS\n\nNotes:\n- The `finding.severity_source == :trailer` conjunct in gate.ex:503 has no test that fails when it is removed. It is not dead code. I built a fixture with `severities: config/unknown_key: info` plus a below-tip `Spec-Ack: config/unknown_key=warning` and ran it through the real CLI: with the clause, stderr is just \"** (Mix) spec.check failed\"; without it, the gate prints \"[CONFIG] Spec-Ack: config/unknown_key=warning resolved from a non-tip commit and will be lost by a squash merge\", which is false advice because Finding.tunable?/1 excludes that code from both config and trailers. So the shipped code is right and the guard is reachable; only the test is missing. Not verdict-changing: no requirement is falsified, and at e18fa05 the resolve_all form handled this case implicitly with no test either.\n- lib/ancora/trailer.ex:15-18 (moduledoc) and :57-59 (the @doc on read/2) still describe non_tip_overrides as \"overrides found only below the tip\" and \"those found only in commits below HEAD\". After this diff a code the tip names at a different severity is in that map, so both sentences are now false about this ticket's own subject matter, and neither mentions nearest-HEAD precedence. The spec says the right thing and the tests pin the right thing, so behavior is unaffected; the only moduledoc assertion in the suite (trailer_test.exs:171) checks for \"self-report\" and \"small-team\", which still hold.\n- The ADR frontmatter date stays 2026-09-03 while the Decision body now carries a precedence decided 2026-09-04. The ticket named that section as an acceptable home, so this is not a scope problem, just a date that no longer matches the body.\n- Three \"fatal: not a git repository\" lines appeared on stderr during the test runs, the known tmp_git_repo.exs cleanup race. No failures attributable to it.",
      "timestamp": "2026-09-04T11:45:51Z"
    },
    {
      "text": "AUDIT: BOUNCE → implementer\nSession: adec463e-8b46-45af-8018-b1cd8b92afc5\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-ancora/adec463e-8b46-45af-8018-b1cd8b92afc5.jsonl\n\nReasoning: I rebuilt every seam from scratch in a disposable detached worktree at a56fd93 and drove real git fixtures through the real CLI. The critic's block is real and worse than it claimed: its UNVERIFIED-BY-READING prediction is now verified at stderr, and the mutation survives not only the three named files but the entire 410-test suite, so the merge gate would not catch it either. The verifier's own seam-2 note reproduces exactly and is a Gap under this epic's every-clause rule, not a Note. The code is right in both places; only tests and two doc sentences are missing. Everything the verifier credited on scope, grammar and the four ancora-sfu states I re-measured myself and confirm.\n\nBounce reason:\n1. lib/ancora/trailer.ex non-tip fold has no covering test; add a CLI test in test/mix/tasks/spec_check_test.exs.\n   Fixture (measured, not hypothesised): base commit anchors sample.subject; C1 changes lib/sample.ex carrying `Spec-Ack: derived/drift=warning`; C2 adds a file carrying `Spec-Ack: derived/drift=info`; the tip adds a file with no trailer; no .spec/config.yml, so the default error is the durable value.\n   Check: status 0, drift severity \"info\" and severity_source \"trailer\" by equality, last stdout line == \"spec.check result=pass\", and `result.stderr == @squash_warning` by equality. Add the squash half if it is cheap: after `git merge --squash`, severity \"error\" / source \"default\" and \"spec.check result=fail tier=branch errors=1 warnings=0\".\n   Mutation the check must kill: delete the `Enum.reverse()` in the `non_tip_overrides` pipeline in lib/ancora/trailer.ex and leave the one in `overrides` intact. Measured under that mutation: the winning severity still resolves nearest at info, non_tip_severity reverts to the oldest commit's warning, the gate's `finding.severity == non_tip_severity` conjunct goes false, stderr is empty, the branch still reports result=pass, and the post-squash trunk run flips to result=fail errors=1 with severity error / source default. Test counts under the mutation today: 67 tests 0 failures on the three named files, and 3 properties / 410 tests 0 failures on the full suite. That is F5 one layer down against the must this diff itself wrote (\"the read result shall identify the nearest valid value below the tip\").\n\n2. The `finding.severity_source == :trailer` clause at lib/ancora/gate.ex:504 has no covering test; add a second CLI test in test/mix/tasks/spec_check_test.exs.\n   Fixture (measured): .spec/config.yml committed at base containing `severities:` with `config/unknown_key: info` plus an unknown top-level key (I used `bogus_top_level_key: true`) so the config/unknown_key finding exists; a below-tip commit carrying `Spec-Ack: config/unknown_key=warning`; a bare tip.\n   Check: status 1 and `result.stderr == \"** (Mix) spec.check failed\\n\"` by equality, which is the whole of stderr on the shipped code.\n   Mutation the check must kill: drop `finding.severity_source == :trailer` from the predicate, leaving the other two conjuncts. Measured under that mutation: stderr gains \"[CONFIG] Spec-Ack: config/unknown_key=warning resolved from a non-tip commit and will be lost by a squash merge; promote it to .spec/config.yml before merging\", which is false advice because Finding.tunable?/1 excludes config/unknown_key from both `severities:` and trailers, so promoting it to config would do nothing. 67 tests, 0 failures under that mutation today. The shipped guard is correct and reachable; the test is the only thing missing.\n\n3. lib/ancora/trailer.ex documentation is now false about this ticket's own subject. The moduledoc `## Scope: base..HEAD` paragraph says read/2 \"unions parsed overrides across every commit in the range\" and \"reports overrides found only below the tip\"; the @doc on read/2 says it returns \"the union of parsed overrides plus those found only in commits below HEAD\". After this diff the fold is nearest-HEAD-wins rather than a plain union, and a code the tip names at a different severity is retained in non_tip_overrides, so \"found only below the tip\" is wrong. Rewrite both to state nearest-HEAD precedence and the by-value below-tip rule, matching ancora.findings.trailer_grammar as this diff amended it.\n   Check: `mix compile --warnings-as-errors` plus the existing moduledoc assertion at test/ancora/trailer_test.exs:171 (\"self-report\", \"small-team\") staying green. No mutation applies; this is doc truth verified by reading against the amended requirement.\n\nNotes (not blocking):\n- Ticket item 2 asked for the key-based non-tip mutation to be proved \"through real git fixtures and the real CLI\". No such CLI fixture exists, in principle, and I am not ordering one. Trailer.read has exactly one call site, Gate.resolve_findings, and non_tip_overrides has exactly one reader. Key-based and value-based differ only when the tip names the code at a different severity, and in that case the tip wins the resolution, so `finding.severity == non_tip_severity` is false and both computations are silent. The unit assertion at test/ancora/trailer_test.exs:154 is the right and only home. Recording this so no later grader re-opens it: the by-value retention is currently dead weight at its only consumer, and it survives because the amended must names the read result rather than the gate. If a future ticket wants it observable, that is a gate change, not a test change.\n- ADR frontmatter date stays 2026-09-03 while the Decision body gained a precedence decided 2026-09-04. Not ordered: all 13 records under .spec/decisions/ carry a single `date:` and none carries an amendment or history section, so there is no repo convention that a body edit bumps it.\n- test/ancora/trailer_test.exs:154 pins nearest-wins and differing-retained but not the equal-dropped half, as the critic noted. The tip-repeat CLI test guards it behaviorally. Not ordered.\n- Three \"fatal: not a git repository\" lines appeared on stderr during my runs, the known tmp_git_repo.exs cleanup race. No failures attributable to it.\n\nConfirmed (credited from the verifier, re-measured by me):\n- Scope: 7 files, all inside Allowed globs, none matching Out-of-Scope. The lib/ancora/gate.ex hunk is 7 insertions / 7 deletions, confined to the body of warn_non_tip_acknowledgments plus one `alias Ancora.Config`; resolve_findings is byte-identical to e18fa05.\n- Grammar unchanged: @severity_tokens and @rejected_severities are untouched by the diff, still info|warning with error and off rejected at parse.\n- The four ancora-sfu states rebuilt from my own fixtures and run against both a compiled e18fa05 worktree and a compiled a56fd93 worktree over the identical repo: equal config silent, stricter config loud, no config loud, tip repeat silent are all byte-identical on stderr under cmp.\n- Baseline green at a56fd93: 67 tests 0 failures across the three named files; 3 properties / 410 tests 0 failures on the full suite.",
      "timestamp": "2026-09-04T11:56:30Z"
    },
    {
      "text": "IMPLEMENTED\nSession: 41r3-i2-adec463e\nLog: /Users/mz/.local/state/agentic-runs/ancora/ancora-41r.3/codex-41r3-i2-adec463e/log\n\nCommit: 7acae4f92081c0ed005d9b46ae83fbe5430f7afa\nFiles touched: lib/ancora/trailer.ex, test/mix/tasks/spec_check_test.exs\nTests added: Mix.Tasks.Spec.CheckTest \"the nearest below-tip acknowledgment warns before squash\" and \"a non-tunable finding does not get squash promotion advice\"\nSpec acknowledgment: none needed; this bounce adds ordered coverage and corrects documentation to match the already-amended ancora.findings.trailer_grammar contract\nVerification tier: change-scoped — mix compile --warnings-as-errors; test/ancora/trailer_test.exs, test/ancora/gate_test.exs, test/mix/tasks/spec_check_test.exs; mix format --check-formatted; mix deps.unlock --check-unused; mix spec.check --base e18fa05; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\n\nSummary: Added the two missing real-CLI fixtures and corrected Trailer.read documentation to state nearest-HEAD precedence and the by-value below-tip rule. Item 1's missing non-tip Enum.reverse mutation ran 1 selected test with 1 failure, then 1 selected test with 0 failures after revert; item 2's missing severity_source guard mutation ran 1 selected test with 1 failure, then 1 selected test with 0 failures after revert; item 3 compiled cleanly and its documentation assertion passed in the 3-selected-test run with 0 failures. The capped final tier passed 69 tests with 0 failures and spec.check result=pass; every epic lesson was checked against the final two-file diff, with no deferred requirement.",
      "timestamp": "2026-09-04T12:03:23Z"
    },
    {
      "text": "TEST_CRITIC: PASS\n\nScope: git -C /Users/mz/src/ancora-41r.3 diff a56fd93..HEAD (HEAD 7acae4f). test/mix/tasks/spec_check_test.exs +67 lines, two new tests. lib/ancora/trailer.ex is doc-only (moduledoc + read/2 @doc rewritten from \"union\" to nearest-wins wording). No test deleted, none loosened, no assertion downgraded from == to =~. Ran the two touched files from the stage worktree: spec_check_test.exs 26 tests 0 failures (26.6s async), trailer_test.exs 11 tests 0 failures.\n\n## Test 1 - \"the nearest below-tip acknowledgment warns before squash\" (test/mix/tasks/spec_check_test.exs:458)\n\nThree commits in the ordered claim: VERIFIED. base -\u003e \"change value\" carrying Spec-Ack: derived/drift=warning -\u003e \"lower severity\" carrying Spec-Ack: derived/drift=info -\u003e \"document branch tip\" with no trailer. Older ack and newer ack name DIFFERENT severities, tip is bare. That is the shape the audit ordered.\n\nstderr pinned by equality naming the nearer value: VERIFIED. @squash_warning (spec_check_test.exs:9) is the literal string \"[CONFIG] Spec-Ack: derived/drift=info resolved from a non-tip commit and will be lost by a squash merge; promote it to .spec/config.yml before merging\\n\". \"info\" is the nearer of the two acks, so the assertion at line 438-equivalent in the new test names the nearest value, not a generic prefix.\n\nDifferent line vs absent line - the audit's specific question, answered NO with a reason:\nUnder mutation M1 (delete the second Enum.reverse in the non_tip fold, lib/ancora/trailer.ex:84-89) non_tip_overrides becomes %{\"derived/drift\" =\u003e :warning}. But the gate's predicate (lib/ancora/gate.ex:504) requires finding.severity == non_tip_severity, and the applied severity is still :info (that comes from the FIRST reverse, untouched by M1). The guard goes false and nothing is printed. So M1 produces an ABSENT line, not a differing one, and the equality assertion rejects it because \"\" != @squash_warning.\nA differing line is unreachable by construction, not by test weakness: the gate only prints when the below-tip value equals the applied severity, so the severity it prints can never disagree with the nearest-wins value. Getting a \"different line\" would require changing gate.ex:504, not the test. I judge the equality assertion the strongest pin available against this predicate. The mutant still dies.\n\nSecond discriminator, independent of M1: assert branch_drift[\"severity\"] == \"info\" and severity_source == \"trailer\". Under mutation M2 (delete the FIRST Enum.reverse, the overrides fold at trailer.ex:76-79) oldest wins and the applied severity becomes \"warning\", so that assertion goes red and the stderr equality goes red too.\n\nUNVERIFIED BY READING: I did not run M1 or M2. For the verifier -\n  M1: in lib/ancora/trailer.ex delete the `|\u003e Enum.reverse()` inside the non_tip_overrides pipeline (the one after `Enum.drop(1)`). Expect \"the nearest below-tip acknowledgment warns before squash\" red on `branch.stderr == @squash_warning` with actual \"\".\n  M2: delete the `|\u003e Enum.reverse()` inside the `overrides` pipeline. Expect the same test red on branch_drift[\"severity\"] == \"info\".\n\n## Test 2 - \"a non-tunable finding does not get squash promotion advice\" (test/mix/tasks/spec_check_test.exs:492)\n\nIs the finding really non-tunable, and how is it provoked: VERIFIED. The fixture writes .spec/config.yml containing `bogus_top_level_key: true`, which fires config/unknown_key (lib/ancora/config.ex:216). config/unknown_key is in @non_tunable (lib/ancora/finding.ex:39) with default severity :warning (finding.ex:71), so Severity.resolve_with_source short-circuits to {:warning, :default} (lib/ancora/severity.ex:94-98). severity_source is :default, never :trailer.\n\nIs the trailer ack below the tip: VERIFIED. Commit \"acknowledge non-tunable finding\" carries Spec-Ack: config/unknown_key=warning, then \"document branch tip\" is bare. config/unknown_key is a registry code so Finding.known?/1 accepts it and it lands in overrides and in non_tip_overrides; the tip has no override so the Map.reject equal-drop keeps it.\n\nThe clause is genuinely load-bearing here, and the `severities: config/unknown_key: info` line is what makes it so. Walking gate.ex:504 with this fixture: durable_severity = Config.severity_for(config, \"config/unknown_key\", subject) = :info, because reduce_severity_entry (config.ex:317-332) accepts the entry - the code is known and \"info\" decodes - even though resolution later ignores it for a non-tunable code. So conjunct 3 (finding.severity != durable_severity) is warning != info = true, and conjunct 2 (finding.severity == non_tip_severity) is warning == warning = true. The ONLY conjunct suppressing the line is `finding.severity_source == :trailer`. That is a precisely built test, not an accidental pass.\n\nAssertion form: `assert result.stderr == \"** (Mix) spec.check failed\\n\"`. Equality, so it excludes the promotion advice and anything else on stderr. Stronger than the \"lacks the exact substring\" the audit would have accepted. status == 1 comes from the config/unknown_key warning being blocking at branch tier.\n\nUNVERIFIED BY READING: M3 - in lib/ancora/gate.ex:504 drop `finding.severity_source == :trailer and` from the predicate. Expect \"a non-tunable finding does not get squash promotion advice\" red on the stderr equality, actual stderr carrying the config/unknown_key=warning promotion line ahead of the Mix failure line.\n\n## Weakness worth recording (recommendation, not a bounce)\n\nTest 2 runs spec.check with --json and never parses the output. Nothing in it pins that the finding is config/unknown_key, resolved at warning, with severity_source \"default\". It cannot silently pass on an empty run - if the fixture stopped producing the finding, status would flip to 0 and the assertion would fail - but the mechanism the test exists to protect is unnamed. The three sibling tests in the same file all assert code, severity and severity_source off last_parseable_json. Suggest matching them:\n\n    report = last_parseable_json(result.stdout)\n    finding = Enum.find(report[\"all_findings\"], \u0026(\u00261[\"code\"] == \"config/unknown_key\"))\n    assert finding[\"severity\"] == \"warning\"\n    assert finding[\"severity_source\"] == \"default\"\n\nThat also documents in the test why the clause fires, which currently lives only in config.ex and finding.ex.\n\n## Previous critic's open note - still true\n\nThe equal-dropped half of non_tip_overrides (the Map.reject at lib/ancora/trailer.ex:89) is still pinned only behaviorally, by the CLI test \"repeating the non-tip acknowledgment on the tip stays silent\" (spec_check_test.exs:527) asserting stderr == \"\". trailer_test.exs has no unit case where the tip repeats the same severity; its nearest case, \"the nearest override wins and keeps a differing non-tip value\" (trailer_test.exs:152), uses warning below and info on the tip, so it exercises only the keep branch. The branch is covered, just one level further out than it needs to be. Mutation for the verifier if you want it confirmed: M4 - delete the `|\u003e Map.reject(...)` line; expect the tip-repeat CLI test red on stderr == \"\".\n\n## Minor\n\nThe newer of the two acknowledgment commits writes a file named \"older.md\" (its body reads \"The nearer acknowledgment lowers the severity.\"). The filename says the opposite of the commit's role and will mislead the next reader of this fixture. Suggest nearer.md.\n\nTest 1 also re-runs the whole squash half already covered by \"a non-tip acknowledgment changes the result after squash\" (spec_check_test.exs:415). About twenty duplicated lines that add no new discrimination. Harmless, and I would leave it if the intent is a self-contained scenario.\n\n## Rule check\n\nETC-CORE-001 async: module runs async, ExUnit reported 26.6s async / 0.00s sync. ETC-CORE-005 no Process.sleep anywhere in the added lines; both tests synchronize through run_mix_subprocess returning. ETC-CORE-007 both names state precondition and outcome. ETC-ISO-002 roots come from the case's tmp fixture, no shared paths. ETC-ORG-002 both assert observable output (exit status, stdout verdict line, stderr text, JSON findings), not internal calls.",
      "timestamp": "2026-09-04T12:09:17Z"
    },
    {
      "text": "VERIFY: PASS\nSession: adec463e-8b46-45af-8018-b1cd8b92afc5\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-ancora/adec463e-8b46-45af-8018-b1cd8b92afc5.jsonl\n\nBounce re-verification of 7acae4f, delta-scoped per step 12. The fix is one commit over a56fd93 touching lib/ancora/trailer.ex (8 insertions, 6 deletions, doc strings only) and test/mix/tasks/spec_check_test.exs (67 insertions, 0 deletions). I rebuilt every mutation myself in a disposable detached worktree at 7acae4f and threw it away; the stage tree was never edited.\n\nMechanical checks:\n- Files touched (e18fa05..HEAD): 7\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0\n- Zero `Spec-Ack:` trailers in e18fa05..HEAD.\n\nOrdered items: all three made, not paraphrased.\n- Item 1 MADE. test/mix/tasks/spec_check_test.exs:460 \"the nearest below-tip acknowledgment warns before squash\" builds the auditor's fixture exactly: anchored base, C1 changing lib/sample.ex with `Spec-Ack: derived/drift=warning`, C2 adding a file with `Spec-Ack: derived/drift=info`, a bare tip, no .spec/config.yml. It pins status 0, severity \"info\", severity_source \"trailer\", last stdout line `spec.check result=pass`, and `branch.stderr == @squash_warning` by equality. It also carries the squash half the auditor called optional: real `git merge --squash` onto main, then status 1, severity \"error\" / source \"default\", `spec.check result=fail tier=branch errors=1 warnings=0`, and stderr `\"** (Mix) spec.check failed\\n\"`. Mutation (delete only the `Enum.reverse()` in the `non_tip_overrides` pipeline, leave the `overrides` one intact): 26 tests, 1 failure, failing on `branch.stderr == @squash_warning`, left `\"\"`, right the squash-loss warning. That is the critic's UNVERIFIED-BY-READING prediction now killed at stderr. Reverted: 26 tests, 0 failures.\n- Item 2 MADE. test/mix/tasks/spec_check_test.exs:507 \"a non-tunable finding does not get squash promotion advice\" commits `.spec/config.yml` with `severities: config/unknown_key: info` plus `bogus_top_level_key: true` at base, a below-tip `Spec-Ack: config/unknown_key=warning`, and a bare tip. It pins status 1 and `result.stderr == \"** (Mix) spec.check failed\\n\"` by equality. Mutation (drop `finding.severity_source == :trailer`, keep the other two conjuncts): 26 tests, 1 failure, stderr gaining exactly \"[CONFIG] Spec-Ack: config/unknown_key=warning resolved from a non-tip commit and will be lost by a squash merge; promote it to .spec/config.yml before merging\". I checked the advice really is false: `@non_tunable` at lib/ancora/finding.ex:39 is `MapSet.new([\"config/unknown_key\", \"config/invalid_value\"])` and `Finding.tunable?/1` is its negation, so promoting that code to config would do nothing. Reverted: 26 tests, 0 failures.\n- Item 3 MADE, and documentation-only proved by execution rather than by reading. The moduledoc now says read/2 \"applies parsed overrides with the commit nearest `HEAD` winning when commits name the same code\" and \"reports the nearest value below the tip when the tip omits that code or names a different severity\"; the @doc says \"with the commit nearest `HEAD` winning for each code\" and \"includes the nearest value below the tip unless the tip repeats that severity\". Both match ancora.findings.trailer_grammar as this diff amended it. No-behavior-change proof: I compiled Ancora.Trailer at HEAD and again with lib/ancora/trailer.ex swapped to its a56fd93 content in the same tree, and `:beam_lib.md5/1` on the two .beam files is identical, DBFC9285E3EC1E12AF00A3D9490D7AF6 both times. Docs live in a separate chunk, so equal code md5 is the compiled-function-body comparison the work order asked for.\n\nSemantic checks:\n- Spec gate: spec.check result=pass\n  (checked subjects=7 requirements=89 errors=0 warnings=0; branch base=e18fa05 changed_files=7 findings=7 total error=0 warning=0 info=7; stderr empty; exit 0)\n- Requirements coverage:\n  - ancora.findings.trailer_grammar (must): covered by test/ancora/trailer_test.exs:154 for nearest-wins and differing-retained, and behaviorally by test/mix/tasks/spec_check_test.exs:460 for the nearest-below-tip half. Would fail if the below-tip fold reverted to oldest-wins (:460 sees empty stderr where the squash-loss warning belongs) or if the winning fold reverted (:154 gets :warning where :info is asserted). Both killed below. Seams: none. init_git_repo/commit_all shell real git through System.cmd; run_mix_subprocess spawns the real mix binary.\n  - ancora.gate.acknowledgment_clears (must): covered by test/mix/tasks/spec_check_test.exs:385, :415, :460, :507, :527, :553, :633. Would fail if the gate emitted a squash-loss line when the tip supplies the winning acknowledgment (:385 pins stderr by equality), if it stopped emitting one when the below-tip value differs from the durable value (:460, :633), or if it emitted promotion advice for a non-tunable code (:507). Seams: none.\n- Original three mutations, re-run at 7acae4f, all still killed:\n  1. Oldest-wins fold (delete the `Enum.reverse()` in the `overrides` pipeline): 2 failures, spec_check_test.exs:385 on `result.status == 1` (left 0) and trailer_test.exs:154 on `overrides[\"derived/drift\"] == :info` (left :warning).\n  2. Key-based non-tip (`Map.drop(overrides, Map.keys(tip_overrides))` in place of the value `Map.reject`): 1 failure, trailer_test.exs:154, left `%{}` right `%{\"derived/drift\" =\u003e :warning}`. Still zero CLI failures, exactly as the auditor recorded and declined to order further.\n  3. Old second-`Severity.resolve_all` predicate restored verbatim: 1 failure, spec_check_test.exs:385, stderr gaining the \"[CONFIG] Spec-Ack: derived/drift=info ...\" line for a code the tip declared at warning.\n- Four ancora-sfu stderr states: still byte-identical to e18fa05. Argued from measurement rather than re-measured from scratch, and the argument is closed. a56fd93..HEAD touches only lib/ancora/trailer.ex and test/mix/tasks/spec_check_test.exs, lib/ancora/gate.ex is byte-identical across that delta, and the Trailer code md5 is identical at a56fd93 and HEAD. So HEAD's runtime behavior equals a56fd93's, where the auditor established byte-identity with e18fa05 under cmp. The four states' pinning tests (:527 tip repeat silent, :553 promoted, :633 stricter config, plus the equal-config case) are all green in the 69-test baseline.\n- Intent-level scope concerns: none. The delta adds no dependency, touches no mix.exs or mix.lock, adds no tip-commit announcement, reads no merge body, and HEAD's parent is a56fd93 whose parent is e18fa05, so no rebase.\n- Targeted verification: pass. Stage worktree `mix spec.check --base e18fa05`, and `mix test test/ancora/trailer_test.exs test/ancora/gate_test.exs test/mix/tasks/spec_check_test.exs` -\u003e 69 tests, 0 failures. All mutation runs were in the disposable worktree.\n- Whitelist adherence: change-scoped + diff-touched tests + spec gate only; compile/format/credo/deps re-establishment: not run\n- Corpus-scoped claims (the two rewritten doc blocks): 5 claims validated, 4 by execution. The `git log \u003cbase\u003e..HEAD --format=%B%x00` shell string matches the Git.run call in read/2; nearest-HEAD precedence, nearest-below-tip reporting, and the tip-repeat drop are each pinned by a test that dies under its own mutation; \"self-report\" and \"small-team\" survive and keep trailer_test.exs:171 green. No falsehoods found.\n- Full-suite verification: not run by verifier (by design)\n- Audit-expanded scope: none. Both new tests and the doc rewrite land inside the ticket's own Allowed globs.\n\nVerdict: PASS\n\nNotes:\n- Restoring the old resolve_all predicate leaves `alias Ancora.Config` unused, and the resulting compiler warning lands on subprocess stderr and breaks seven stderr-pinned tests for the wrong reason. I removed the alias with the predicate to get a clean signal, which is why mutation 3 reports 1 failure and not 7. Anyone re-running that mutation should do the same or they will misread the count.\n- The item-2 test does not discriminate the old resolve_all form: under mutation 3 it stays green. That is expected, since the old form handled the non-tunable case implicitly. The test exists to pin the new predicate's `severity_source` conjunct, and mutation 2 shows it does that.\n- No \"fatal: not a git repository\" lines appeared in any of my runs this pass.",
      "timestamp": "2026-09-04T12:12:12Z"
    }
  ],
  "priority": 1,
  "status": "in_progress",
  "title": "The oldest Spec-Ack trailer beats the tip and silences the squash-loss warning",
  "type": "bug",
  "updated_at": "2026-09-04T12:12:12Z"
}
