{
  "assignee": "Manuel Zubieta",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-09-04T11:03:32Z",
  "description": "Advances: ancora.findings, ancora.scaffold\n\nFindings: F7 (High, api H3 + spec L3). Individual report: 20260904-061300-api.md beside ~/vaults/rmbrs-vault/code_review/critical/ancora/2026-09-04/ancora-xla-post-ship-hardening/20260904-061300-consolidated.md.\n\nDeliverable:\nREADME.md:53-59 and docs/migration.md:32-41 tell users to promote a Spec-Ack trailer into config using an optional `requirement:` key in an overrides entry, naming Argos, Engage, Builder and Atlas as the audience. Ancora.Config has no such key: parse_override_entry (config.ex:392) reads only subject, code, severity and reason and drops anything else with no diagnostic, so the override silently applies subject-wide instead of requirement-scoped and the gate suppresses more than the author asked for. The key belongs to ancora-gg8.1 on the other epic branch and is not in this tree.\nFix: reword README.md and docs/migration.md to say overrides are scoped to a subject and a code in this release (and, if you keep a forward reference, say plainly that the key is not accepted yet and what happens if written); make Config reject unsupported keys inside an overrides entry with a config finding (config/invalid_value or config/unknown_key; state which in the spec) so a written `requirement:` key is a loud error, not a silent widening; drop the forward-reference paragraph from .spec/decisions/ancora.decision.durable_acknowledgments.md; check priv/spec_init/agents/SKILL.md and priv/spec_init/README.md for the same instruction and fix them if present.\n\nVerification:\n- Tests to pass: test/ancora/config_test.exs, test/ancora/scaffold/docs_test.exs, test/mix/tasks/spec_check_test.exs\n- Behaviors to demonstrate:\n  1. A .spec/config.yml with an overrides entry carrying `requirement:` (or any unknown key) makes `mix spec.check` report a config finding naming the key and the entry, and the override is NOT applied. Mutation: restore the silent drop; the test must fail.\n  2. A well-formed overrides entry still applies exactly as before (regression).\n  3. README.md and docs/migration.md no longer instruct writing a `requirement:` key (docs test asserts the promotion paragraph's content by normalized text, not a fragile substring). Mutation: reintroduce the sentence; the test must fail.\n- Spec edit required in-diff: ancora.findings (config family) shall state that unknown keys inside an overrides entry are a config finding and the entry is not applied; ancora.scaffold only if the scaffold docs change. Decision-record edit: ancora.decision.durable_acknowledgments loses the forward reference and records the 1.x scope of overrides.\n- Verification cadence:\n  - Iteration tier: compile-graph-scoped checks only (`mix test --stale` or the named files) plus `mix spec.validate`; run `mix spec.check --base e18fa05` as a dev-loop check as soon as the spec edit exists, before either capped final-tier run.\n  - Final tier: immediately before IMPLEMENTED, `mix compile --warnings-as-errors`, the named test files, `mix format --check-formatted`, `mix deps.unlock --check-unused`, `mix spec.check --base e18fa05`, run-until-green. Full suite runs at the orchestrator's merge gate.\n  - Verifier tier: `mix spec.check --base e18fa05` plus targeted selections; no full suite.\n- Standing bounce-class checks:\n  - Production call site named for each new callable surface, with the proving test that exercises that production path\n  - Every conditional clause covered by a test that fails under the named mutation; prove each ordered test both ways and report the numbers\n  - Spec surfaces kept fresh: the substantive requirement or scenario edit named below lands in the same diff with its decision-record edit; never a Spec-Ack trailer; never weaken a must\n  - Fixtures never call production functions (use test/support or System.cmd); under file-scoped derivation a stray call binds a subject\n  - Grader scratch discipline applies to you too: work only in this worktree, address it by absolute path\n- Call-site Integration: Config.load/1 is called once from Preflight; prove through the CLI fixture, not the config unit test alone.\n\nOut of Scope (files):\n- lib/ancora/trailer.ex, lib/ancora/gate.ex, lib/ancora/severity.ex, lib/ancora/review/**, lib/ancora/parser.ex, lib/ancora/verifier.ex, lib/ancora/git/**, lib/ancora/derive/**, lib/mix/tasks/**\nAllowed touches (this ticket may modify):\n- lib/ancora/config.ex\n- README.md, docs/migration.md, priv/spec_init/agents/SKILL.md, priv/spec_init/README.md, priv/spec_init/config.yml.eex (documentation of overrides only)\n- test/ancora/**, test/mix/tasks/**, test/support/**\n- .spec/specs/ancora.findings.spec.md, .spec/specs/ancora.scaffold.spec.md (substantive edits named above; never weakening a must)\n- .spec/decisions/ancora.decision.durable_acknowledgments.md\n- Any .spec/specs/\u003csubject\u003e.spec.md that `mix spec.check --base e18fa05` names in a derived/drift, derived/growth or derived/shrink finding caused by THIS ticket's own diff (binding-aware clause: derivation is file-scoped, so acknowledgment edits only, one substantive sentence describing what this ticket changed as that subject observes it, verified by execution; if the subject observes nothing, say so in the completion record and stop)\n- .spec/decisions/** (every spec edit requires a decision-record edit in the same diff; write a real contract statement, never a hollow line)\n\nOut of Scope (intent):\n- Do not implement requirement-scoped overrides (ancora-gg8.1 owns that on its branch). Do not change the severities map handling. Do not rebase; base stays e18fa05.\n\nMerge gate:\n- [ ] Code compiles cleanly\n- [ ] Tests listed above pass\n- [ ] Spec verification passes (`mix spec.check --base e18fa05` result=pass)\n- [ ] Scope check: diff touches only Allowed globs (mechanical)\n- [ ] Auditor APPROVE comment on the ticket",
  "id": "ancora-41r.5",
  "labels": [
    "critical",
    "phase:implementing"
  ],
  "parent": "ancora-41r",
  "comments": [
    {
      "text": "IMPLEMENTED\nSession: 41r5-i1-adec463e\nLog: /Users/mz/.local/state/agentic-runs/ancora/ancora-41r.5/codex-41r5-i1-adec463e/log\n\nCommit: aa5e8c174cb1c3aba809e5bac703d8807a9630d1\nFiles touched: .spec/decisions/ancora.decision.durable_acknowledgments.md, .spec/specs/ancora.findings.spec.md, .spec/specs/ancora.scaffold.spec.md, README.md, docs/migration.md, lib/ancora/config.ex, test/ancora/config_test.exs, test/ancora/scaffold/docs_test.exs, test/mix/tasks/spec_check_test.exs\nTests added: Ancora.ConfigTest \"per-subject overrides an unknown key fires config/unknown_key and the override is not applied\"; Ancora.Scaffold.DocsTest \"acknowledgment promotion documents the override scope accepted by 1.x\"; Mix.Tasks.Spec.CheckTest \"an unknown override key is reported and the override is not applied\"\nSpec acknowledgment: substantive requirement and scenario edits in ancora.findings and ancora.scaffold; ancora.decision.durable_acknowledgments updated; no Spec-Ack trailer used\nVerification tier: change-scoped — mix compile --warnings-as-errors; test/ancora/config_test.exs, test/ancora/scaffold/docs_test.exs, test/mix/tasks/spec_check_test.exs; mix format --check-formatted; mix deps.unlock --check-unused; mix spec.check --base e18fa05; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\n\nSummary: Config now rejects unknown override-entry keys with non-tunable config/unknown_key findings, ignores those entries, and the real CLI fixture proves the rejected override cannot suppress its subject finding. README and migration promotion guidance now documents the subject-and-code scope accepted by Ancora 1.x, pinned by normalized paragraph assertions and matching spec and decision edits. Mutation proofs were behavior 1 at 22 tests, 1 failure mutated and 22 tests, 0 failures restored; behavior 2 at 15 tests, 1 failure mutated and 15 tests, 0 failures restored; behavior 3 at 3 tests, 1 failure mutated and 3 tests, 0 failures restored.",
      "timestamp": "2026-09-04T11:28:05Z"
    },
    {
      "text": "TEST_CRITIC: PASS\n\nReviewed aa5e8c1 over e18fa05. Three added tests, all additive; no existing\nassertion in config_test.exs, docs_test.exs or spec_check_test.exs was\nloosened or deleted. Ran test/ancora/config_test.exs and\ntest/ancora/scaffold/docs_test.exs (18 tests, 0 failures) and\ntest/mix/tasks/spec_check_test.exs:382 (1 test, 0 failures) from the stage\nworktree.\n\nPER-ASSERTION VERDICT\n\n1. CLI test proves NOT-applied. PASS.\n   test/mix/tasks/spec_check_test.exs:382 does not stop at \"a config finding\n   appeared\". It asserts the suppressed subject finding still fires with its\n   default severity: code derived/unanchored_subject, subject sample.subject,\n   severity \"warning\". Had the entry been applied it would render \"info\" and\n   drop out of the gated set. Backed by exit status 1 and the fail verdict\n   last on stdout.\n   Reachability is established, not assumed: `assert Enum.any?(...)` cannot\n   pass vacuously, and the file passes, so both findings are genuinely present\n   in the parsed JSON report.\n   The one thing reading alone cannot settle is whether \"warning\" is a live\n   discriminator or a value that would appear even if overrides never applied\n   through the subprocess path at all. That is answered by the positive\n   control at test/ancora/gate_test.exs:751, where a well-formed override on\n   the same code downgrades sample.a to :info and drops it from\n   report.findings while sample.b stays :warning. Same Gate.check path\n   spec.check drives. I consider the discriminator sound.\n   MUTATION FOR THE VERIFIER IF YOU WANT IT AIRTIGHT IN THIS FILE: drop the\n   `unknown_keys` clause from parse_override/3 in lib/ancora/config.ex so the\n   entry is accepted. The second Enum.any? at line 405 must fail (severity\n   flips to \"info\").\n\n2. Config unit test names the key and the entry. PASS WITH A GAP.\n   test/ancora/config_test.exs:164 asserts `config.overrides == []` by\n   equality, which is the strongest available not-applied proof at this\n   layer. It names the key as `~s(\"requirement\")` (quoted form, so it matches\n   the inspected key, not an incidental prose occurrence) and names the entry\n   via the \"atlas.web.sessions\" substring inside the inspected map.\n   On \"entry index\": the repo spec scenario\n   ancora.findings.scenario.override_unknown_key requires the finding to name\n   \"requirement and the entry\", not an index, and the implementation names\n   the whole inspected entry. No index exists to assert. I judged against the\n   spec.\n   GAP (warning, not blocking): both the unit test and the CLI test match the\n   entry only by its subject id. A mutation that shortened the finding key to\n   name just the subject and dropped the inspected entry would survive both,\n   while violating \"naming ... the entry\". Tightening: assert\n   `finding.key =~ \"in overrides entry\"` or match the code and severity\n   fields too.\n\n3. Docs test normalization and paragraph content. PASS.\n   test/ancora/scaffold/docs_test.exs:27 collapses all whitespace through\n   String.split |\u003e Enum.join(\" \") and asserts full-paragraph EQUALITY against\n   both README.md and docs/migration.md. Not a fragile substring, and not a\n   needle either: re-adding any `requirement:` instruction inside that\n   paragraph changes the string and fails.\n   DELETION DOES NOT PASS. promotion_paragraphs/1 uses Enum.find, which\n   returns nil when the paragraph is gone; normalize(nil) calls\n   String.split(nil), which raises ArgumentError (verified by running it).\n   The test errors rather than silently passing. It fails loudly, though as\n   an ArgumentError rather than a readable assertion diff.\n   GAP (recommendation): the guard is scoped to that one paragraph in each\n   file. A `requirement:` instruction re-added as a NEW paragraph elsewhere\n   in README.md or docs/migration.md would pass. Neither file carries a\n   blanket refute on the key. Both files currently mention \"requirement\" only\n   in unrelated senses (requirement ids, the code map rows), so a blanket\n   refute would need to target `requirement:` in an overrides context.\n\n4. Well-formed entry still applies. PASS, and it was already there.\n   test/ancora/config_test.exs:243 \"an override applies only to the named\n   subject\" is untouched and asserts subject.a resolves to :info with\n   severity_source :config, subject.b stays :warning/:default, and\n   subject_status returns :acknowledged for a only. The end-to-end twin at\n   gate_test.exs:751 is also untouched. The new unknown-key tests sit beside\n   these rather than replacing them.\n\n5. Source-text pinning of lib/ancora/config.ex. NONE ADDED.\n   All three new tests assert on behavior (loaded config, resolved severity,\n   JSON report, file content of docs). The pre-existing moduledoc needle test\n   in the \"co-versioning note\" describe block is unchanged, and the\n   moduledoc edit in this commit left its three needles (mix.lock, same PR,\n   dependency) intact.\n\n6. Weakening check. NONE.\n   git diff e18fa05..HEAD -- test/ is purely additive: 24 lines in\n   config_test.exs, 27 in docs_test.exs, 34 in spec_check_test.exs. No\n   deletions, no relaxed matchers, no removed tags.\n\nUNVERIFIED BY READING\nNone that block. The single reachability question (item 1) was resolved by\nrunning the file, since the assertion form cannot pass vacuously. The named\nmutation is offered as belt-and-braces for the verifier, not as an\noutstanding doubt.",
      "timestamp": "2026-09-04T11:33:15Z"
    },
    {
      "text": "VERIFY: FAIL\nSession: adec463e-8b46-45af-8018-b1cd8b92afc5\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-ancora/adec463e-8b46-45af-8018-b1cd8b92afc5.jsonl\n\nMechanical checks:\n- Files touched: 9\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0\n\nSemantic checks:\n- Spec gate: spec.check result=pass\n  (checked subjects=7 requirements=89 errors=0 warnings=0; branch base=e18fa05 changed_files=9 findings=7 total error=0 warning=0 info=7)\n- Requirements coverage (must requirements this diff writes or rewrites):\n  - ancora.findings.per_subject_overrides: PARTIALLY COVERED. The \"entry is ignored\"\n    half is pinned twice, at test/ancora/config_test.exs:165 (assert config.overrides == [])\n    and at test/mix/tasks/spec_check_test.exs:382 through the real CLI subprocess (the\n    subject's derived/unanchored_subject finding still fires at warning, so the override\n    did not suppress it). Both fail under the named mutation. The \"Any other entry key\"\n    half is NOT covered — see Gaps. Seams: none; the CLI test shells out to a real\n    `mix spec.check --root \u003cfixture\u003e` subprocess against a real git fixture, and the\n    config test drives Ancora.Config.load/2 on a real .spec/config.yml on disk.\n  - ancora.scaffold.readme_commitments: covered by test/ancora/scaffold/docs_test.exs:29.\n    Would fail if README's promotion paragraph stopped naming the subject-and-code scope\n    or reacquired the `requirement:` instruction; proven by reintroducing the sentence.\n    Seams: none, it reads the shipped README.md.\n  - ancora.scaffold.migration_doc: covered by the same test at docs_test.exs:42, which\n    asserts docs/migration.md's promotion paragraph equals README's after whitespace\n    normalization. Proven the same way against migration.md. Seams: none.\n  - ancora.findings.config_schema (non-tunability, unchanged but load-bearing here):\n    test/ancora/finding_test.exs:164 asserts Finding.tunable?/1 is false for\n    config/unknown_key. I also proved it end to end: see Targeted verification.\n- Intent-level scope concerns: none. No requirement-scoped override was implemented,\n  severities parsing is untouched, mix.exs and mix.lock are untouched, base is still\n  e18fa05, and the commit carries no Spec-Ack trailer.\n- Targeted verification: pass except the Gap below. Commands run, all from absolute\n  paths in a disposable `git worktree add --detach` copy (removed at the end) or against\n  a throwaway fixture repo under the scratchpad:\n  - mix spec.check --base e18fa05 (stage worktree)\n  - mix test test/ancora/config_test.exs test/ancora/scaffold/docs_test.exs\n    test/mix/tasks/spec_check_test.exs -\u003e 40 tests, 0 failures\n  - Mutation A, restore the silent drop in parse_override_entry: config_test +\n    spec_check_test go 37 tests / 2 failures mutated, 37 tests / 0 failures restored.\n  - Mutation B, reintroduce the `requirement:` sentence in README: docs_test goes\n    3 tests / 1 failure mutated, 3 tests / 0 failures restored. Same again for\n    docs/migration.md separately: 3 tests / 1 failure, then 3 tests / 0 failures.\n  - Real-CLI fixture proofs (`mix spec.check --root \u003cfixture\u003e --base \u003csha\u003e` from the\n    stage worktree against a hand-built git project, not the test helpers):\n    - overrides entry carrying `requirement:` -\u003e config/unknown_key fires naming\n      \"requirement\" and the whole entry map, and derived/unanchored_subject still\n      reports at WARNING for that subject, so the override was not applied.\n      Verdict `spec.check result=fail tier=branch errors=0 warnings=3`.\n    - the same entry with the extra key removed -\u003e derived/unanchored_subject drops to\n      info and leaves the warning list. The well-formed override still applies.\n    - an unrelated unknown key (`scope: whole-repo`) -\u003e also fires config/unknown_key,\n      naming \"scope\". The implementation is genuinely general.\n    - non-tunability, both routes: `severities: {config/unknown_key: \"off\"}` in the\n      fixture config leaves the finding at WARNING; a commit trailer\n      `Spec-Ack: config/unknown_key=off` is rejected outright (\"[CONFIG] Spec-Ack:\n      rejected severity ... downgrade only\") and `Spec-Ack: config/unknown_key=info`\n      is accepted as grammar but leaves the finding at WARNING. It cannot be turned off.\n  - Scaffold sweep: grep across the tree for `requirement:` inside an overrides context\n    returns nothing outside the new .spec/specs/ancora.findings.spec.md scenario text.\n    priv/spec_init/agents/SKILL.md:61-68, priv/spec_init/config.yml:10-15 and\n    priv/spec_init/README.md:16 all show or describe four-key overrides only, so those\n    two files correctly needed no edit.\n  - ADR check: .spec/decisions/ancora.decision.durable_acknowledgments.md lost the\n    forward-reference paragraph and now records the 1.x scope in the Decision body. The\n    ticket asked for exactly that, so the body edit is authorized, not a rewrite of a\n    point-in-time decision.\n- Whitelist adherence: change-scoped + diff-touched tests + spec gate + contract adds\n  only; compile/format/credo/deps re-establishment: not run\n- Corpus-scoped claims (doc-shaped diff): 11 claims validated, 8 by execution. Every\n  finding code, config key and CLI invocation named in the changed README, migration and\n  spec text was fed through the real parser or the real `mix spec.check`. No falsehood\n  found. Two advisory observations below.\n- Full-suite verification: not run by verifier (by design)\n- Audit-expanded scope: none, this is the first verification pass.\n\nVerdict: FAIL\n\nGaps:\n- ancora.findings.per_subject_overrides: the requirement this ticket rewrote says \"Any\n  other entry key shall produce `config/unknown_key` naming the key and entry, and the\n  entry shall be ignored\", and the ticket's own behavior 1 says \"carrying `requirement:`\n  (or any unknown key)\". Both tests that exercise it use the key `requirement` and\n  nothing else. I loosened lib/ancora/config.ex:400 from\n  `MapSet.difference(@known_override_keys)` to\n  `MapSet.intersection(MapSet.new([\"requirement\"]))`, so the parser rejects only that one\n  literal key and silently drops every other unknown key exactly as before the fix, and\n  all three named test files still pass: 40 tests, 0 failures. The defining word \"any\n  other\" is unpinned. To be clear about what this is and is not: the shipped\n  implementation is correct, I proved by real CLI run that `scope: whole-repo` fires\n  config/unknown_key too. The gap is in the tests, not the code, and the fix is one\n  fixture line, either a second unknown key alongside `requirement:` in the config_test\n  entry or a non-`requirement` key in the spec_check_test entry.\n\nNotes:\n- .spec/decisions/ancora.decision.durable_acknowledgments.md:29 says \"Unknown entry keys\n  are config errors\". config/unknown_key defaults to `warning`, and \"error\" is a severity\n  token elsewhere in this vocabulary. The claim is true in the plain-English sense, the\n  gate does fail on it, but a reader could expect errors=1. Advisory only.\n- README.md:55-62 replaced the only place the README showed the trailer syntax\n  `Spec-Ack: \u003ccode\u003e=\u003cinfo|warning\u003e`. It now survives in README prose only as\n  \"`Spec-Ack:` trailers\". The grammar is still in docs/migration.md:105 and in\n  priv/spec_init/agents/SKILL.md, and no requirement obliges the README to carry it, so\n  this does not change the verdict.\n- The ADR's Decision paragraph now wraps raggedly around the inserted sentences\n  (.spec/decisions/ancora.decision.durable_acknowledgments.md:29-31, \"A `Spec-Ack:`\n  trailer / remains a temporary development convenience. When an / applied trailer\").\n  Cosmetic.",
      "timestamp": "2026-09-04T11:38:21Z"
    },
    {
      "text": "VERIFY: FAIL\nSession: adec463e-8b46-45af-8018-b1cd8b92afc5\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-ancora/adec463e-8b46-45af-8018-b1cd8b92afc5.jsonl\n\nFollow-up measurements on the two checks raised after the test critic's PASS. The\nverdict is unchanged, still FAIL on the Gap already filed. One of the two adds a\nsecond Gap under the same requirement; the other lands as a Note. Both were measured\nin a disposable `git worktree add --detach` copy, removed afterwards, and against the\nthrowaway CLI fixture.\n\nCheck 1 — does the finding message actually have to name the entry?\nTwo mutations, opposite results:\n- 1a, drop the entry entirely: `key = \"#{keys}\"`. Both tests catch it, 37 tests /\n  2 failures, because the subject id reaches the message only through the inspected\n  entry map, so removing the map removes the subject too. The critic's read of the\n  assertions is right, and this is the loosening the guard does hold against.\n- 1b, keep the subject but drop the entry:\n  `key = \"#{keys} in an overrides entry for #{Map.get(entry, \"subject\")}\"`. 37 tests /\n  0 failures. The guard does not hold.\nThe spec sentence this ticket wrote says the finding shall name \"the key and entry\".\nUnder 1b it names the key and one field of the entry, and the difference is\nobservable, not theoretical. I put two bad entries on the same subject in the fixture\nand ran the real CLI. The shipped code emits two findings distinguishable by their\nfull entry maps, one naming `derived/unanchored_subject` with `requirement`, the other\nnaming `derived/drift` with `scope`. Under 1b both would read \"in an overrides entry\nfor sample.subject\" and the author could not tell which of the two to fix. The word\n\"entry\" is load-bearing and no test pins it. Judged against the wording as written\nthis is a Gap, filed below under the same requirement as the first one.\n\nCheck 2 — is the one-paragraph docs guard wide enough?\nMeasured, not assumed. I re-added the instruction as a NEW paragraph in both files,\nabove \"## Deprecated 1.x grammar\" in README.md and above the `spec-exceptions`\nparagraph in docs/migration.md: \"For the narrowest durable acknowledgment, add an\noptional `requirement:` key to the overrides entry naming the exact requirement id you\nare acknowledging.\" docs_test stays at 3 tests / 0 failures. `promotion_paragraphs/1`\nfinds only the paragraph starting \"`Spec-Ack:` trailers\", so F7's exact defect can\ncome back anywhere else in either file unseen. The other README test asserts needles\nand refutes \"proof\" and \"verified behavior\", and carries no `requirement:` needle.\nI also swept the current tree: README.md has one \"requirement\" occurrence (line 51,\n\"the subject's requirements or scenarios\"), docs/migration.md has ten, all either\nrequirement ids or finding codes containing the word, and priv/spec_init/README.md,\nagents/SKILL.md, AGENTS.md.eex and config.yml have none in an overrides context. So\nthere is no live falsehood today. This lands as a Note rather than a Gap because the\ntwo spec requirements as written, ancora.scaffold.readme_commitments and\nancora.scaffold.migration_doc, oblige the docs to STATE the 1.x scope, and the guard\nproves that positive statement; and because the ticket itself specified the\nparagraph-content assertion and named the in-paragraph mutation, which does fail.\nWorth saying anyway since the owner is already reopening these files for the Gaps:\na whole-file `refute content =~ \"`requirement:`\"` on both paths is one line and closes\nthe class.\n\nGaps (both under ancora.findings.per_subject_overrides, both test-side; the shipped\nimplementation is correct by execution in every case I ran):\n- \"Any other entry key\": both tests use only `requirement`. Loosening\n  lib/ancora/config.ex:400 from `MapSet.difference(@known_override_keys)` to\n  `MapSet.intersection(MapSet.new([\"requirement\"]))` leaves 40 tests / 0 failures\n  across the three named files, while every other unknown key goes back to being\n  silently dropped.\n- \"naming the key and entry\": no test distinguishes naming the entry from naming the\n  entry's subject. Mutation 1b above survives at 37 tests / 0 failures.\nBoth close with fixture edits, not code changes. A second unknown key in one entry\ncovers the first; asserting on something entry-specific, the code or the reason,\ncovers the second.\n\nNotes:\n- The docs guard's paragraph scope, measured above. Advisory.\n- The three Notes on the earlier comment stand unchanged.",
      "timestamp": "2026-09-04T11:41:50Z"
    },
    {
      "text": "AUDIT: BOUNCE → implementer\nSession: adec463e-8b46-45af-8018-b1cd8b92afc5\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-ancora/adec463e-8b46-45af-8018-b1cd8b92afc5.jsonl\n\nReasoning: I reproduced both of the verifier's mutations from scratch in a disposable\nworktree at aa5e8c1 and both survive, so the FAIL holds and it is not the verifier's\nerror. Baseline over the three named files is 40 tests, 0 failures. Loosening\nlib/ancora/config.ex:399 from MapSet.difference(@known_override_keys) to\nMapSet.intersection(MapSet.new([\"requirement\"])) leaves 40 tests, 0 failures. Replacing\nthe finding key at lib/ancora/config.ex:404 with\nkey = \"#{keys} in an overrides entry for #{Map.get(entry, \"subject\")}\" also leaves\n40 tests, 0 failures. The shipped implementation is right in both cases; the tests do\nnot hold it there. That is two unpinned halves of the one must this ticket rewrote,\nancora.findings.per_subject_overrides, so it bounces to the implementer, not the verifier.\n\nI did not stop at ordering a remedy. I built each ordered check and ran it against the\nmutation it has to kill, because the verifier's first suggested remedy does not work.\nAdding a second unknown key to the existing entry and asserting on the second key name\nstill passes under the loosening mutation, 40 tests / 0 failures, because the finding key\nembeds inspect(entry) and the inspected map renders every key including the one the\nmutated parser ignored. Asserting a key name proves nothing about which key was rejected.\nThe checks below are the ones I measured working.\n\nOn the critic's docs note I measured rather than reasoned. I re-added F7's own sentence\nas a new paragraph above \"## Deprecated 1.x grammar\" in README.md and docs_test stayed at\n3 tests / 0 failures. The exact defect this ticket exists to remove can walk back in\nunseen. I am ordering the one-line close because the ticket's behavior 3 is a property of\nthe files, not of one paragraph, and because it costs a line. It is the smallest of the\nthree.\n\nBounce reason:\n1. test/ancora/config_test.exs, \"any other entry key\" is unpinned. Add a second test in\n   the \"per-subject overrides\" describe block whose entry's ONLY unknown key is not\n   `requirement` (I used `scope: whole-repo` alongside the four accepted keys), asserting\n   `config.overrides == []` and that a config/unknown_key finding's message matches\n   ~s(\"scope\"). Check that proves it: the two config tests plus\n   test/mix/tasks/spec_check_test.exs. Mutation it must kill: change\n   MapSet.difference(@known_override_keys) at lib/ancora/config.ex:399 to\n   MapSet.intersection(MapSet.new([\"requirement\"])). Measured with this test present:\n   38 tests / 1 failure mutated, 38 tests / 0 failures restored. Do NOT close this by\n   adding a second unknown key to the existing entry; that form survives the mutation for\n   the reason above, and I ran it.\n2. test/ancora/config_test.exs:164, \"naming the key and entry\" is unpinned. In the\n   existing unknown-key test add one assertion that the finding message carries a field of\n   the entry that is not derivable from its subject. `finding.message =~ \"integration-only\"`\n   (the reason) is what I ran. Check that proves it: the same selection. Mutation it must\n   kill: replace lib/ancora/config.ex:404 with\n   key = \"#{keys} in an overrides entry for #{Map.get(entry, \"subject\")}\". Measured:\n   38 tests / 1 failure mutated, 38 tests / 0 failures restored. Without it that mutation\n   passes clean, and with two bad entries on one subject the author cannot tell which\n   entry to fix.\n3. test/ancora/scaffold/docs_test.exs, the promotion guard is scoped to one paragraph per\n   file. Add a whole-file test refuting the literal \"requirement:\" in both File.read!(@readme)\n   and File.read!(@migration), tagged to both scaffold requirements. Neither file contains\n   that literal today, verified by grep, and priv/spec_init/** contains it nowhere, so the\n   refute is not a false positive. Check that proves it: docs_test.exs. Mutation it must\n   kill: re-add the F7 instruction as a NEW paragraph anywhere in either file. Measured:\n   4 tests / 1 failure mutated, 4 tests / 0 failures on the clean tree; the current guard\n   gives 3 tests / 0 failures under the same mutation.\n\nAll three are fixture and assertion edits. Do not touch lib/ancora/config.ex; it is\ncorrect as shipped and I confirmed the general behavior holds. Re-run the final tier per\nthe ticket cadence before re-posting IMPLEMENTED, and report each ordered check both ways\nwith numbers.\n\nNotes (not blocking):\n- .spec/decisions/ancora.decision.durable_acknowledgments.md:31 says unknown entry keys\n  \"are config errors\". config/unknown_key defaults to warning (lib/ancora/finding.ex:71)\n  and \"error\" is a severity token in this vocabulary, so a reader may expect errors=1 when\n  the gate reports warnings. \"are config findings that fail the gate\" would be exact. Worth\n  a word while the file is open, but the sentence is true in plain English and no\n  requirement obliges the change.\n- README.md no longer shows the Spec-Ack grammar anywhere; it survives at\n  docs/migration.md:105 and priv/spec_init/agents/SKILL.md:55. ancora.scaffold.readme_commitments\n  does not oblige the README to carry it, so this is not a defect. Flagging it because the\n  README is now the only entry-point doc that names the trailer without showing its shape.\n- The ADR Decision paragraph wraps raggedly around the inserted sentences, lines 29-31.\n  Cosmetic, fix it if you are in the file.\n- Scaffold sweep is clean and needs nothing: no \"requirement:\" literal anywhere in\n  README.md, docs/migration.md or priv/spec_init/**, and the SKILL.md and config.yml\n  override examples show four keys only.\n\nBounces used: 1 of 2. Mechanical-remedy 1 of 2.",
      "timestamp": "2026-09-04T11:50:46Z"
    },
    {
      "text": "IMPLEMENTED\nSession: 41r5-i2-adec463e\nLog: /Users/mz/.local/state/agentic-runs/ancora/ancora-41r.5/codex-41r5-i2-adec463e/log\n\nCommit: 1523912763c647e51c8ccd8a98c6bf567126b5a9\nFiles touched: test/ancora/config_test.exs, test/ancora/scaffold/docs_test.exs\nTests added: Ancora.ConfigTest \"an unknown key other than requirement is rejected\"; Ancora.Scaffold.DocsTest \"acknowledgment docs do not teach unsupported requirement-scoped overrides\"; strengthened Ancora.ConfigTest \"an unknown key fires config/unknown_key and the override is not applied\"\nSpec acknowledgment: none needed for this assertion-only bounce commit; the existing substantive ancora.findings and ancora.scaffold edits remain in the diff, with no Spec-Ack trailer\nVerification tier: change-scoped — mix compile --warnings-as-errors; test/ancora/config_test.exs, test/ancora/scaffold/docs_test.exs, test/mix/tasks/spec_check_test.exs; mix format --check-formatted; mix deps.unlock --check-unused; mix spec.check --base e18fa05; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\n\nSummary: Added a standalone non-requirement unknown-key fixture, strengthened the existing finding assertion with its entry-specific reason, and added a whole-file guard against unsupported requirement-scoped override instructions. Ordered check 1 produced 38 tests / 1 failure mutated and 38 tests / 0 failures restored; ordered check 2 produced 38 tests / 1 failure mutated and 38 tests / 0 failures restored. Ordered check 3 produced 4 tests / 1 failure mutated and 4 tests / 0 failures restored; all current epic lessons were checked against the final two-file diff.",
      "timestamp": "2026-09-04T12:00:15Z"
    }
  ],
  "priority": 1,
  "status": "in_progress",
  "title": "README and the migration guide promote acknowledgments with an override key Config silently drops",
  "type": "bug",
  "updated_at": "2026-09-04T12:00:15Z"
}
