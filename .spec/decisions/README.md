# Decisions

One file per ADR, named `<id>.md`. Frontmatter: `id`, `status`, `date`,
`affects:` (subject or requirement ids). Sections: Context, Decision,
Consequences. A subject id documents broad scope. Only an accepted ADR that
names the exact requirement id authorizes deleting that requirement or
downgrading it from `must` to `should`.
